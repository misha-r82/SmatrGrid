<UserControl x:Class="SmartGrid.TagControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SmartGrid"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <local:TagTemplateSelector x:Key="TagTemplateSelector"/>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Name="txtHeader" Text="{Binding Header}"/>
        <ListBox Name="lstMain" Grid.Row="1" ItemsSource="{Binding}" ItemTemplateSelector="{StaticResource TagTemplateSelector}" PreviewMouseDown="ListBox_OnPreviewMouseDown"
                  Drop="ListBox_Drop" AllowDrop="True">
            <ListBox.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="TextWrapping" Value="Wrap"/>
                    <Setter Property="AcceptsReturn" Value="True"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
                <DataTemplate x:Key="NodeTemplate">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="10"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding Header}" Grid.Row="0" HorizontalAlignment="Stretch" MouseDown="UIElement_OnMouseDown"/>
                        <TextBox Text="{Binding Val}" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" Grid.ColumnSpan="3"/>
                        <StackPanel Name="panelDrag" PreviewMouseDown="UIElement_OnPreviewMouseDown" 
                                    Background="Blue" Grid.Column="2" Grid.RowSpan="2"/>
                    </Grid>
                </DataTemplate>
                <DataTemplate x:Key="HeadNodeTemplate">
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Text="{Binding Header}" Grid.Row="0"/>
                        <Button Name ="btnAdd"  Content="+" Grid.Column="1" Click="BtnAdd_OnClick"/>
                    </Grid>
                </DataTemplate>
            </ListBox.Resources>

        </ListBox>
    </Grid>

</UserControl>

